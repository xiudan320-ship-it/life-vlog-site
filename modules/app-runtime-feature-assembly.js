import { createAuthController } from "./auth-controller.js";
import { createAnniversaryController } from "./anniversary-controller.js";
import { createDiaryComposerController } from "./diary-composer-controller.js";
import { createFoodWheelController } from "./food-wheel-controller.js";
import { createGratitudeController } from "./gratitude-controller.js";
import { createOfflineCacheController } from "./offline-cache-controller.js";
import { createPushController } from "./push-controller.js";
import { createTrashController } from "./trash-controller.js";

/**
 * Assemble the feature controllers that depend on the shared core graph.
 * Lazy page proxies come from the route boundary; this module only creates
 * feature controllers and exposes their stable actions to the route layer.
 */
export function createFeatureControllerAssembly({
  elements,
  state,
  services,
  config,
  core,
  pages,
}) {
  const {
    secretController,
    recipeController,
    wishlistController,
    shoppingController,
    weekendController,
    familySettingsController,
    offlineSettingsController,
    dataSafetyController,
    callLoaded,
  } = pages;
  const {
    diaryUploadQueue,
    diaryRepository,
    householdRepository,
    notificationRepository,
    mediaCacheService,
    preferenceStore,
    assetController,
    photoFavorites,
    cloudflareRequest,
    secretDataService,
  } = services;
  const {
    anniversaryKey,
    autoDiaryCacheItemLimit,
    defaultDiaryCacheMb,
    defaultFoodOptions,
    defaultSecretCacheMb,
    diaryCacheMbKey,
    diaryDraftKey,
    diaryMediaCacheName,
    foodOptionsKey,
    legacyMediaCacheName,
    maxCacheMb,
    mediaCachePolicyKey,
    minCacheMb,
    pageSize,
    photoCommentPreviewLimit,
    photoFeedCacheKey,
    secretCacheMbKey,
    secretItemsCacheKey,
    secretMediaCacheName,
    thanksColorKey,
    thanksColors,
    defaultThanksColor,
    productionUrl,
    r2UploadEndpoint,
  } = config;
  const {
    awardExperience,
    awardDailyExperience,
    canManageItem,
    cleanupStoredImagePaths,
    closeMobileDiaryPage,
    closePhotoDialog,
    compressImage,
    confirmAction,
    createTrashItem,
    deleteR2Object,
    escapeHtml,
    formatFileSize,
    getAuthorName,
    getCurrentImageLimit,
    getFinalTitle,
    getPhotoImages,
    getPhotoLabel,
    isAdmin,
    getProfileAvatarUrl,
    getSessionBoundEmail,
    getSessionDisplayName,
    getUploadFileNameBase,
    getDefaultSecretSortOrder: getSecretSortOrder,
    getSortedPhotos,
    isMissingCloudSchema,
    loadCachedAvatarUrl,
    loadFamilyLevelProfiles,
    loadPhotos,
    loadGratitudeNotes,
    normalizeSecretImages,
    normalizeUuid,
    openMobileDiaryPage,
    openNotificationsPanel,
    openPhoto,
    processDiaryUploadQueue,
    refreshCacheInfo,
    renderAccountAvatar,
    renderGallery,
    renderMobileDiaryComments,
    renderSecretGallery,
    renderSettingsSummary,
    rollbackTrashItem,
    saveCachedAvatarUrl,
    setGlobalStatus,
    setHint,
    setSecretStatus,
    showMiniToast,
    synchronizeAccountData,
    synchronizeAnniversaries,
    synchronizeWeekendPlans,
    switchPage,
    updateCloudSyncStatus,
    updateSessionDisplayName,
    usernameToEmail,
    isNetworkLikeError,
    getLocalDateKey,
    normalizeLoginDateKey,
    isYesterdayLoginDate,
    normalizeNickname,
    normalizeTheme,
    loadTheme,
    applyTheme,
    saveThanksColorPreference,
    syncExistingPushSubscription,
    renderOverview,
    renderFoodWheel,
    setActiveSettingsSection,
    openPushDestination,
    getTodayExperienceStorageKey,
    saveExperience,
    saveRechargeTotal,
    getDailyLoginReward,
  } = core;
  const loadSecretItems = (...args) => secretDataService.load(...args);

  const {
    addSecretImageLinks,
    appendSecretAlbumImages,
    closeSecretAlbumContextMenu,
    closeSecretFolderContextMenu,
    createSecretFolder,
    deleteActiveSecretFolder,
    deleteCurrentSecretTag,
    deleteSecretDialogImage,
    deleteSecretItem,
    deleteSelectedSecretImages,
    getImageFilesFromClipboard,
    getSecretAlbumFilterTags,
    getSecretFavoriteEntries,
    handleSecretPaste,
    moveSecretAlbum,
    moveSelectedSecretImage,
    moveSelectedSecretImagesToAlbum,
    openSecretAlbumFolderDialog,
    openSecretItem,
    openSecretLinkedDiary,
    removeSecretPhotoTagFromSelection,
    renderSecretFolderControls,
    renderSecretLinkedPhotoOptions,
    returnToSecretItem,
    saveSecretAlbumEdit,
    saveSecretItem,
    setSecretExpanded,
    setSelectedSecretCover,
    toggleDiaryImageFullscreen,
    toggleDialogImageFullscreen,
    updateSecretDialogImage,
    updateSecretPreview,
  } = secretController;
  const {
    applyCoverUrl: applyRecipeCoverUrl,
    edit: editRecipe,
    handleCoverPaste: handleRecipeCoverPaste,
    load: loadRecipes,
    remove: deleteRecipe,
    render: renderRecipes,
    resetForm: resetRecipeForm,
    save: saveRecipes,
    setExpanded: setRecipeExpanded,
    setStatus: setRecipeStatus,
    submit: saveRecipe,
    updateCoverPreview: updateRecipeCoverPreview,
  } = recipeController;
  const {
    applyImageUrl: applyWishImageUrl,
    clearImagePreview: clearWishImagePreview,
    closeCompleteDialog: closeWishCompleteDialog,
    edit: editWish,
    handleImagePaste: handleWishImagePaste,
    openCompleteDialog: openWishCompleteDialog,
    remove: deleteWish,
    removeImage: removeWishImage,
    render: renderWishes,
    resetForm: resetWishForm,
    saveCompletionState: saveWishCompletionState,
    setExpanded: setWishlistExpanded,
    setImagePreview: setWishImagePreview,
    setStatus: setWishlistStatus,
    submit: saveWish,
    submitCompletion: submitWishCompletion,
    toggle: toggleWish,
    updateImagePreview: updateWishImagePreview,
  } = wishlistController;
  const { render: renderShopping } = shoppingController;
  const {
    addCompletionFiles: addWeekendCompletionFiles,
    addCompletionLinks: addWeekendCompletionLinks,
    addFiles: addWeekendFiles,
    addImageLinks: addWeekendImageLinks,
    clearCompletionState: clearWeekendCompletionState,
    clearImageState: clearWeekendImageState,
    closeCompletionDialog: closeWeekendCompletionDialog,
    edit: editWeekendPlan,
    handleImagePaste: handleWeekendImagePaste,
    load: loadWeekendPlans,
    openCompletionDialog: openWeekendCompletionDialog,
    remove: deleteWeekendPlan,
    removeCompletionEntry: removeWeekendCompletionEntry,
    removeImageEntry: removeWeekendImageEntry,
    render: renderWeekendPlans,
    renderCompletionPreviews: renderWeekendCompletionPreviews,
    renderImagePreviews: renderWeekendImagePreviews,
    resetForm: resetWeekendForm,
    save: saveWeekendPlans,
    saveCompletion: saveWeekendCompletion,
    setExpanded: setWeekendExpanded,
    setStatus: setWeekendStatus,
    submit: saveWeekendPlan,
    toggle: toggleWeekendPlan,
  } = weekendController;
  const {
    renderFamilyDialog,
    renderSettingsFamilyPanel,
    readSignupInviteCode,
    bindSettingsFamilyActions,
    setActiveSettingsSection: setFamilySettingsSection,
    openSettingsDialog,
    openSettingsChildDialog,
    reopenSettingsAfterChildDialog,
    closeSettingsDialog,
    refreshSharedContent,
    createFamily,
    addFamilyMember,
    respondFamilyInvitation,
    removeFamilyMember,
  } = familySettingsController;
  const {
    changeCacheLimit,
    saveCacheLimitFromDialog,
    applyCacheLimitPreset,
    ensureCacheManagementUi,
    downloadOfflinePool,
    clearCachePool,
  } = offlineSettingsController;
  const {
    downloadFamilyBackup,
    renderTrashItems,
    downloadCloudBackup,
    renderCloudBackups,
    createCloudBackupNow,
    backfillLegacyThumbnails,
    ensureDataSafetyUi,
    createSettingsSection,
    runOfflineDiagnostics,
    ensureStabilitySettingsUi,
  } = dataSafetyController;
  const renderUploadCenterIfLoaded = (...args) =>
    callLoaded(dataSafetyController, "renderUploadCenter", ...args);

  const trashState = {
    get cloudDb() { return state.cloudDb; },
    get session() { return state.session; },
    get photos() { return state.photos; },
    set photos(value) { state.photos = value; },
  };
  const trashController = createTrashController({
    state: trashState,
    householdRepository,
    diaryRepository,
    photoFavorites,
    getPhotoImages,
    normalizeSecretImages,
    cleanupStoredImagePaths,
    showMiniToast,
    setGlobalStatus,
    getPhotoLabel,
    isAdmin,
    renderGallery,
    loadPhotos,
    loadSecretItems,
    loadGratitudeNotes,
    synchronizeWeekendPlans,
    synchronizeAnniversaries: (...args) => core.synchronizeAnniversaries(...args),
    synchronizeAccountData,
    renderTrashItems: (...args) => callLoaded(dataSafetyController, "renderTrashItems", ...args),
  });
  const {
    createTrashItem: createTrashItemFromController,
    snapshotPhotoCommentsForTrash,
    rollbackTrashItem: rollbackTrashItemFromController,
    confirmWishDeletion,
    getTrashImagePaths,
    loadTrashItems,
    restoreTrashItem,
    permanentlyDeleteTrashItem,
    getTrashOwnershipLabel,
    deletePhoto,
  } = trashController;

  const diaryComposerController = createDiaryComposerController({
    elements,
    queue: diaryUploadQueue,
    repository: diaryRepository,
    assets: assetController,
    vlogMode: core.vlogMode,
    draftKey: diaryDraftKey,
    getCloudDatabase: () => state.cloudDb,
    getSession: () => state.session,
    getCurrentImageLimit,
    getFinalTitle,
    getUploadFileNameBase,
    formatFileSize,
    escapeHtml,
    setStatus: core.setStatus,
    awardExperience,
    loadPhotos,
    switchPage,
    renderUploadCenter: renderUploadCenterIfLoaded,
  });
  const {
    getQueuedUploads: getQueuedDiaryUploads,
    isProcessing: isDiaryUploadQueueProcessing,
    processQueue: processDiaryUploadQueueFromController,
    removeQueuedUpload: removeQueuedDiaryUpload,
    setExpanded: setUploadExpanded,
  } = diaryComposerController;

  const authController = createAuthController({
    elements,
    endpoint: r2UploadEndpoint,
    getDatabase: () => state.cloudDb,
    getSession: () => state.session,
    usernameToEmail,
    getRedirectUrl: () => core.resolveRedirectUrl(productionUrl),
    setHint,
    getBoundEmail: getSessionBoundEmail,
    renderSettingsSummary,
    isMissingCloudSchema,
    closeMobileDiaryPage,
    clearSecretUnlockState: core.clearSecretUnlockState,
  });
  const {
    changePassword,
    confirmEmailBinding,
    confirmEmailPasswordReset,
    login: loginWithPassword,
    logout,
    requestEmailBinding,
    requestEmailPasswordReset,
    resetEmailBindingDialog,
    resetEmailRecoveryUi,
    resetForgottenPassword,
    saveRecoveryKey,
    signup: signupWithPassword,
  } = authController;

  const offlineCacheController = createOfflineCacheController({
    elements,
    preferenceStore,
    mediaCacheService,
    keys: {
      photoFeed: photoFeedCacheKey,
      secretItems: secretItemsCacheKey,
      diaryCapacity: diaryCacheMbKey,
      secretCapacity: secretCacheMbKey,
      policy: mediaCachePolicyKey,
    },
    cacheNames: {
      diary: diaryMediaCacheName,
      secret: secretMediaCacheName,
      legacy: legacyMediaCacheName,
    },
    limits: {
      minMb: minCacheMb,
      maxMb: maxCacheMb,
      defaultDiaryMb: defaultDiaryCacheMb,
      defaultSecretMb: defaultSecretCacheMb,
      autoDiaryItems: autoDiaryCacheItemLimit,
      metadataItems: config.metadataCacheItemLimit,
      commentPreviews: photoCommentPreviewLimit,
      pageSize,
    },
    getSession: () => state.session,
    getPhotos: () => state.photos,
    setPhotos: (items) => { state.photos = items; },
    getSortedPhotos,
    getPhotoImages,
    getPhotoCommentPreviews: () => state.photoCommentPreviewMap,
    setPhotoCommentPreviews: (map) => { state.photoCommentPreviewMap = map; },
    setShowingCachedFeed: (value) => { state.showingCachedFeed = value; },
    setVisiblePhotoCount: (value) => { state.visiblePhotoCount = value; },
    getActivePage: () => state.activePage,
    renderGallery,
    setGlobalStatus,
    getSecretItems: () => state.secretItems,
    setSecretItems: (items) => { state.secretItems = items; },
    setSecretCloudAvailable: (value) => { state.secretCloudAvailable = value; },
    renderSecretGallery,
    setSecretStatus,
    normalizeSecretImages,
    getDefaultSecretSortOrder: getSecretSortOrder,
    getProfileAvatarUrl,
    getAccountProfile: () => state.accountProfile,
    getFamilyMembers: () => state.familyMemberMap,
    getFamilyLevelProfiles: () => state.familyLevelProfiles,
    renderSettingsSummary,
    formatFileSize,
  });
  const {
    cacheMedia: cacheOfflineMedia,
    clear: clearAppCache,
    collectDiaryUrls: collectDiaryOfflineMediaUrls,
    collectSecretUrls: collectSecretOfflineMediaUrls,
    getCapacityStorageKey: getCacheCapacityStorageKey,
    getPhotoFeedStorageKey: getPhotoFeedCacheStorageKey,
    getPolicyKey: getMediaCachePolicyKey,
    getSecretStorageKey: getSecretItemsCacheStorageKey,
    getStats: getAppCacheStats,
    isUnmetered: isClearlyUnmeteredConnection,
    loadCapacityMb: loadCacheCapacityMb,
    loadPolicy: loadMediaCachePolicy,
    refreshInfo: refreshCacheInfoFromController,
    renderCachedPhotoFeed,
    renderCachedSecretItems,
    renderStats: renderCacheStats,
    saveCapacityMb: saveCacheCapacityMb,
    savePhotoFeed: savePhotoFeedCache,
    savePolicy: saveMediaCachePolicy,
    saveSecretItems: saveSecretItemsCache,
    schedule: scheduleOfflineMediaCache,
    shouldAutoCache: shouldAutoCacheMedia,
  } = offlineCacheController;

  const familySettingsState = {
    get familyInfo() { return state.familyInfo; },
    get familyMembers() { return state.familyMembers; },
    get familyInvitations() { return state.familyInvitations; },
    get session() { return state.session; },
    get activeSettingsSection() { return state.activeSettingsSection; },
    set activeSettingsSection(value) { state.activeSettingsSection = value; },
    get returnToSettingsAfterDialog() { return state.returnToSettingsAfterDialog; },
    set returnToSettingsAfterDialog(value) { state.returnToSettingsAfterDialog = value; },
    get cloudDb() { return state.cloudDb; },
    get recipes() { return state.recipes; },
    set recipes(value) { state.recipes = value; },
    get wishes() { return state.wishes; },
    set wishes(value) { state.wishes = value; },
  };
  const dataSafetyState = {
    get session() { return state.session; },
    get cloudDb() { return state.cloudDb; },
    get photos() { return state.photos; },
    get secretItems() { return state.secretItems; },
    get activeUploadTasks() { return state.activeUploadTasks; },
    get photosLoadPromise() { return state.photosLoadPromise; },
    get notificationsLoadPromise() { return state.notificationsLoadPromise; },
    get secretLoadPromise() { return state.secretLoadPromise; },
  };

  const foodWheelController = createFoodWheelController({
    elements,
    storageKey: foodOptionsKey,
    defaultOptions: defaultFoodOptions,
    repository: householdRepository,
    getSession: () => state.session,
    getRecipes: () => state.recipes,
    getOptions: () => state.foodOptions,
    setOptions: (nextOptions) => {
      state.foodOptions = nextOptions;
      state.accountProfile.foodOptions = [...nextOptions];
    },
    canPersist: () => Boolean(
      state.cloudDb && state.cloudSyncAvailable && state.foodOptionsCloudAvailable
    ),
    closeMobileDiaryPage,
    closePhotoDialog,
  });
  const {
    addOption: addFoodOption,
    close: closeFoodWheel,
    getStorageKey: getFoodOptionsStorageKey,
    getWheelOptions,
    loadOptions: loadFoodOptions,
    open: openFoodWheel,
    persistOptions: persistFoodOptions,
    removeOption: removeFoodOption,
    render: renderFoodWheelFromController,
    saveOptionsCache: saveFoodOptionsCache,
    spin: spinFoodWheel,
  } = foodWheelController;
  state.foodOptions = loadFoodOptions();

  const pushController = createPushController({
    elements,
    request: cloudflareRequest,
    notificationRepository,
    diaryRepository,
    getSession: () => state.session,
    getDatabase: () => state.cloudDb,
    getPhotos: () => state.photos,
    prependPhoto: (photo) => state.photos.unshift(photo),
    loadNotifications: core.loadNotifications,
    openNotificationsPanel,
    setActiveSettingsSection,
    switchPage,
    openPhoto,
    showToast: showMiniToast,
  });
  const {
    disable: disableWebPush,
    enable: enableWebPush,
    ensureSettingsPage: ensurePushSettingsPage,
    openDestination: openPushDestinationFromController,
    refreshSettings: refreshPushSettings,
    registerWorker: registerAppShellWorker,
    syncExistingSubscription: syncExistingPushSubscriptionFromController,
  } = pushController;

  const anniversaryController = createAnniversaryController({
    elements,
    storageKey: anniversaryKey,
    repository: householdRepository,
    getSession: () => state.session,
    getDatabase: () => state.cloudDb,
    getItems: () => state.anniversaries,
    setItems: (items) => { state.anniversaries = items; },
    isCloudAvailable: () => state.anniversaryCloudAvailable,
    setCloudAvailable: (available) => { state.anniversaryCloudAvailable = available; },
    getAuthorName,
    canManageItem,
    normalizeUuid,
    awardExperience,
    confirmAction,
    createTrashItem: createTrashItemFromController,
    rollbackTrashItem: rollbackTrashItemFromController,
    isMissingCloudSchema,
  });
  const {
    edit: editAnniversary,
    load: loadAnniversaries,
    remove: deleteAnniversary,
    render: renderAnniversaries,
    resetForm: resetAnniversaryForm,
    save: saveAnniversaries,
    setFormExpanded: setAnniversaryFormExpanded,
    submit: saveAnniversary,
    synchronize: synchronizeAnniversariesFromController,
  } = anniversaryController;

  const gratitudeController = createGratitudeController({
    elements,
    storageKey: thanksColorKey,
    allowedColors: thanksColors,
    defaultColor: defaultThanksColor,
    repository: householdRepository,
    getSession: () => state.session,
    getDatabase: () => state.cloudDb,
    getNotes: () => state.gratitudeNotes,
    getProfileColor: () => state.accountProfile.thanksColor,
    setProfileColor: (color) => {
      state.accountProfile.thanksColor = color;
    },
    isCloudAvailable: () => state.thanksColorCloudAvailable,
    setCloudAvailable: (available) => { state.thanksColorCloudAvailable = available; },
    getAuthorName,
    canManageItem,
    loadNotes: loadGratitudeNotes,
    awardExperience,
    isMissingCloudSchema,
    confirmAction,
    createTrashItem: createTrashItemFromController,
    rollbackTrashItem: rollbackTrashItemFromController,
  });
  const {
    edit: editGratitudeNote,
    getSelectedColor: getSelectedThanksColor,
    loadColor: loadThanksColor,
    normalizeColor: normalizeThanksColor,
    persistColor: persistThanksColorToCloud,
    remove: deleteGratitudeNote,
    render: renderGratitudeNotes,
    resetForm: resetGratitudeForm,
    saveColor: saveThanksColorPreferenceFromController,
    setSelectedColor: setSelectedThanksColor,
    submit: saveGratitudeNote,
  } = gratitudeController;

  return Object.freeze({
    pages,
    secretActions: {
      addSecretImageLinks,
      appendSecretAlbumImages,
      closeSecretAlbumContextMenu,
      closeSecretFolderContextMenu,
      createSecretFolder,
      deleteActiveSecretFolder,
      deleteCurrentSecretTag,
      deleteSecretDialogImage,
      deleteSecretItem,
      deleteSelectedSecretImages,
      getImageFilesFromClipboard,
      getSecretAlbumFilterTags,
      getSecretFavoriteEntries,
      handleSecretPaste,
      loadSecretItems,
      moveSecretAlbum,
      moveSelectedSecretImage,
      moveSelectedSecretImagesToAlbum,
      openSecretAlbumFolderDialog,
      openSecretItem,
      openSecretLinkedDiary,
      removeSecretPhotoTagFromSelection,
      renderSecretFolderControls,
      renderSecretLinkedPhotoOptions,
      returnToSecretItem,
      saveSecretAlbumEdit,
      saveSecretItem,
      setSecretExpanded,
      setSelectedSecretCover,
      toggleDiaryImageFullscreen,
      toggleDialogImageFullscreen,
      updateSecretDialogImage,
      updateSecretPreview,
    },
    recipeActions: {
      applyRecipeCoverUrl,
      editRecipe,
      handleRecipeCoverPaste,
      loadRecipes,
      deleteRecipe,
      renderRecipes,
      resetRecipeForm,
      saveRecipes,
      setRecipeExpanded,
      setRecipeStatus,
      saveRecipe,
      updateRecipeCoverPreview,
    },
    wishlistActions: {
      applyWishImageUrl,
      clearWishImagePreview,
      closeWishCompleteDialog,
      editWish,
      handleWishImagePaste,
      openWishCompleteDialog,
      deleteWish,
      removeWishImage,
      renderWishes,
      resetWishForm,
      saveWishCompletionState,
      setWishlistExpanded,
      setWishImagePreview,
      setWishlistStatus,
      saveWish,
      submitWishCompletion,
      toggleWish,
      updateWishImagePreview,
    },
    shoppingActions: { renderShopping },
    weekendActions: {
      addWeekendCompletionFiles,
      addWeekendCompletionLinks,
      addWeekendFiles,
      addWeekendImageLinks,
      clearWeekendCompletionState,
      clearWeekendImageState,
      closeWeekendCompletionDialog,
      editWeekendPlan,
      handleWeekendImagePaste,
      loadWeekendPlans,
      openWeekendCompletionDialog,
      deleteWeekendPlan,
      removeWeekendCompletionEntry,
      removeWeekendImageEntry,
      renderWeekendPlans,
      renderWeekendCompletionPreviews,
      renderWeekendImagePreviews,
      resetWeekendForm,
      saveWeekendPlans,
      saveWeekendCompletion,
      setWeekendExpanded,
      setWeekendStatus,
      saveWeekendPlan,
      toggleWeekendPlan,
    },
    familySettingsActions: {
      renderFamilyDialog,
      renderSettingsFamilyPanel,
      readSignupInviteCode,
      bindSettingsFamilyActions,
      setActiveSettingsSection: setFamilySettingsSection,
      openSettingsDialog,
      openSettingsChildDialog,
      reopenSettingsAfterChildDialog,
      closeSettingsDialog,
      refreshSharedContent,
      createFamily,
      addFamilyMember,
      respondFamilyInvitation,
      removeFamilyMember,
    },
    offlineSettingsActions: {
      changeCacheLimit,
      saveCacheLimitFromDialog,
      applyCacheLimitPreset,
      ensureCacheManagementUi,
      downloadOfflinePool,
      clearCachePool,
    },
    dataSafetyActions: {
      downloadFamilyBackup,
      renderTrashItems,
      downloadCloudBackup,
      renderCloudBackups,
      createCloudBackupNow,
      backfillLegacyThumbnails,
      ensureDataSafetyUi,
      createSettingsSection,
      runOfflineDiagnostics,
      renderUploadCenter: renderUploadCenterIfLoaded,
      ensureStabilitySettingsUi,
    },
    trashController,
    trashActions: {
      createTrashItem: createTrashItemFromController,
      snapshotPhotoCommentsForTrash,
      rollbackTrashItem: rollbackTrashItemFromController,
      confirmWishDeletion,
      getTrashImagePaths,
      loadTrashItems,
      restoreTrashItem,
      permanentlyDeleteTrashItem,
      getTrashOwnershipLabel,
      deletePhoto,
    },
    diaryComposerController,
    diaryComposerActions: {
      getQueuedDiaryUploads,
      isDiaryUploadQueueProcessing,
      isNetworkLikeError,
      processDiaryUploadQueue: processDiaryUploadQueueFromController,
      removeQueuedDiaryUpload,
      setUploadExpanded,
    },
    authController,
    authActions: {
      changePassword,
      confirmEmailBinding,
      confirmEmailPasswordReset,
      loginWithPassword,
      logout,
      requestEmailBinding,
      requestEmailPasswordReset,
      resetEmailBindingDialog,
      resetEmailRecoveryUi,
      resetForgottenPassword,
      saveRecoveryKey,
      signupWithPassword,
    },
    offlineCacheController,
    offlineCacheActions: {
      cacheOfflineMedia,
      clearAppCache,
      collectDiaryOfflineMediaUrls,
      collectSecretOfflineMediaUrls,
      getCacheCapacityStorageKey,
      getPhotoFeedCacheStorageKey,
      getMediaCachePolicyKey,
      getSecretItemsCacheStorageKey,
      getAppCacheStats,
      isClearlyUnmeteredConnection,
      loadCacheCapacityMb,
      loadMediaCachePolicy,
      refreshCacheInfo: refreshCacheInfoFromController,
      renderCachedPhotoFeed,
      renderCachedSecretItems,
      renderCacheStats,
      saveCacheCapacityMb,
      savePhotoFeedCache,
      saveMediaCachePolicy,
      saveSecretItemsCache,
      scheduleOfflineMediaCache,
      shouldAutoCacheMedia,
    },
    familySettingsState,
    dataSafetyState,
    foodWheelController,
    foodWheelActions: {
      addFoodOption,
      closeFoodWheel,
      getFoodOptionsStorageKey,
      getWheelOptions,
      loadFoodOptions,
      openFoodWheel,
      persistFoodOptions,
      removeFoodOption,
      renderFoodWheel: renderFoodWheelFromController,
      saveFoodOptionsCache,
      spinFoodWheel,
    },
    pushController,
    pushActions: {
      disableWebPush,
      enableWebPush,
      ensurePushSettingsPage,
      openPushDestination: openPushDestinationFromController,
      refreshPushSettings,
      registerAppShellWorker,
      syncExistingPushSubscription: syncExistingPushSubscriptionFromController,
    },
    anniversaryController,
    anniversaryActions: {
      editAnniversary,
      loadAnniversaries,
      deleteAnniversary,
      renderAnniversaries,
      resetAnniversaryForm,
      saveAnniversaries,
      setAnniversaryFormExpanded,
      saveAnniversary,
      synchronizeAnniversaries: synchronizeAnniversariesFromController,
    },
    gratitudeController,
    gratitudeActions: {
      editGratitudeNote,
      getSelectedThanksColor,
      loadThanksColor,
      normalizeThanksColor,
      persistThanksColorToCloud,
      deleteGratitudeNote,
      renderGratitudeNotes,
      resetGratitudeForm,
      saveThanksColorPreference: saveThanksColorPreferenceFromController,
      setSelectedThanksColor,
      saveGratitudeNote,
    },
  });
}
