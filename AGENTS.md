1、不保留向后兼容。除非用户明确要求，否则过时实现直接删除，不添加兼容层、不编写 migration、不保留 fallback。

2、选择满足当前需求且不会制造明确架构债的最简单实现。不要预防性抽象，不要增加当前需求不需要的配置层、扩展点或通用框架。

3、复杂系统按层推进。先跑通最小端到端闭环，再逐步增加功能和复杂度。不要为了尚未实现的未来设计，拆掉已经正确运行的路径。

4、组件保持模块化并遵循关注点分离。视图、领域逻辑、数据访问和副作用不要无理由混在同一模块中。

5、优先复用项目已有依赖；现有依赖无法满足需求时，再考虑引入成熟、持续维护的库。没有明确理由，不重复造轮子或自行重写成熟能力。

6、架构决策应满足可预见的长期需求，但不为假设中的未来需求提前增加复杂度。不接受明知需要很快推翻的临时方案。

7、优先参考成熟产品、主流框架、官方文档和业界已验证的解决模式。没有明确收益，不自行发明新的交互模式、架构模式或基础设施方案。

8、新功能和业务逻辑禁止继续堆进 `app.js`。`app.js` 只负责应用初始化、状态桥接和模块装配；新增功能必须按职责放进 `modules/` 下的独立模块，视图、领域逻辑和控制器保持分离。

9、凡涉及 UI 结构、视觉样式、交互、响应式、动效或无障碍的任务，使用 `ui-ux-pro-max` 作为顾问参考：先读取其 `SKILL.md`，按需查询相关 UX 或技术栈规则，并在交付前完成对应检查。其建议不得覆盖本文件规则、项目设计系统或用户的明确要求。

10、仓库任务采用最小上下文原则。

开始任务时固定阅读 `docs/README.md`，并运行 `git status --short`。先通过文件名、关键词或 `rg` 定位相关源码，再按 `docs/README.md` 的任务路由决定是否读取其他文档。

不得在尚未确认任务相关性之前批量读取 `docs/`、完整 CHANGELOG、专项规划、审计或历史恢复文档。

只有发生用户可见行为、系统行为、模块职责、架构、数据流、数据库、接口契约、构建、部署、安全或其他重要工程变化时，才同步更新对应长期文档或 `CHANGELOG.md`。

纯拼写、排版、注释、格式化、无行为变化的小型内部整理，以及仅调整测试描述等不影响系统事实的修改，默认不要求更新 CHANGELOG。

具体文档读取和同步范围以 `docs/README.md` 的任务路由与同步判断表为准；只有规则存在歧义或任务涉及文档、测试、发布、交付流程本身时，再查阅 `docs/CHANGE_WORKFLOW.md`。

11、自动化测试和发布验收使用确定性内存 fixture，不使用真实测试账户、密码、token 或真实业务数据。
